
"use client";

// This component has been renamed to project-tasks-view.tsx to better reflect its purpose.
// This file can be removed in the future. For now, it will just re-export the new component
// to avoid breaking any potential (though unlikely) direct imports.

export { ProjectTasksView as ProjectPlanningView } from './project-tasks-view';


"use client";

// This component is now obsolete.
// Its functionality has been merged into the more flexible NewTaskDialog,
// which now serves as the master dialog for creating projects, tasks, and events.
export function NewProjectDialog() {
  return null;
}

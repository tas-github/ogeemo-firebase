
'use client';

// This component is a copy of FilesView to resolve a build error.
// The functionality is identical, providing a full file management interface.
import { FilesView } from "@/components/files/files-view";

export function ManageFilesView() {
  return <FilesView />;
}

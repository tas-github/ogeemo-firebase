import { allMenuItems } from "@/lib/menu-items";

export default function FilesRedirectPage() {
  // This page is a legacy redirect. The main file manager is at /file-manager
  // and menu items are centrally managed in /lib/menu-items.ts.
  // This file can be considered for cleanup in the future.
  return null;
}
